# Required — GitHub personal access token (needs repo scope)
GH_TOKEN=ghp_...

# Optional: X (Twitter) daily digest posting — set all 4 to enable
# Generate in X Developer Portal under your app's "Keys and tokens" section.
# TWITTER_CONSUMER_KEY=
# TWITTER_CONSUMER_SECRET=
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_TOKEN_SECRET=

# Optional: loop configuration (Docker Compose)
# REPO_URL=https://github.com/vindl/ai-government.git
# REPO_BRANCH=main
# LOOP_MAX_CYCLES=         # empty = unlimited
# LOOP_COOLDOWN=           # seconds between cycles
# LOOP_PROPOSALS=          # proposals per cycle
# LOOP_MODEL=              # Claude model override
# LOOP_MAX_PR_ROUNDS=      # max coder-reviewer rounds per PR
# LOOP_DRY_RUN=true        # skip execution, just propose and triage
# LOOP_SKIP_ANALYSIS=true  # skip government decision analysis
# LOOP_SKIP_IMPROVE=true   # skip self-improvement loop
# LOOP_VERBOSE=true        # verbose logging

# Note: Claude credentials are volume-mounted from ~/.claude (OAuth/Max plan).
# No ANTHROPIC_API_KEY needed if you're logged into Claude Code locally.
